<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container">
    <%= link_to 'Role Based Access Control', root_path, class: 'navbar-brand' %>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav mr-auto">
        <% if @current_user %>
          <li class="nav-item">
            <%= link_to 'Resources', resources_path, class: 'nav-link' %>
          </li>
          <% if @current_user&.super_admin? %>
            <li class="nav-item">
              <%= link_to 'Users', users_path, class: 'nav-link' %>
            </li>
          <% end %>
        <% end %>
      </ul>
    </div>
    <% if @current_user %>
      <div class="ml-auto d-flex align-items-center">
        <div class="mr-3"><span><%= @current_user.name %></span></div>
        <%= link_to 'Logout', logout_path, method: :delete, class: 'btn btn-danger', data: { confirm: 'Are you sure you want to log out?' } %>
      </div>
    <% end %>
  </div>
</nav>
